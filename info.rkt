#lang info

(define collection 'multi)

(define version "0.1")

(define deps '("base"
               "rackunit"))

(define build-deps '("doc-coverage"
                     "scribble-lib"
                     "racket-doc"))
